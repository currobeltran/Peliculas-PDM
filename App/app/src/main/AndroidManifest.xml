<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.peliculaspdm">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PeliculasPDM">
        <activity android:name=".InfoPeliculaListaSeguir"></activity>
        <activity android:name=".InfoPelicula" />
        <activity android:name=".ListadoPeliculas" />
        <activity android:name=".AsistenteDeVoz" />
        <activity android:name=".ObtenerRecomendacion2" />
        <activity
            android:name=".ValorarPelicula"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".InicioSesion">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".PeliculasRelacionadas"
            android:parentActivityName=".ObtenerRecomendacion2" />
        <activity android:name=".SeleccionGenero" />
        <activity android:name=".InicioNoAsistente" />
        <activity android:name=".MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>